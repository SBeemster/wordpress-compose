; Route PHP mail() calls through msmtp.
; --read-envelope-from lets msmtp honour the From: header so the address
; configured in WooCommerce → Settings → Emails is used as the envelope sender.
sendmail_path = "/usr/bin/msmtp -t --read-envelope-from"
