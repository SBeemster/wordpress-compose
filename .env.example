# Copy this file to .env and fill in all values before running docker compose.

# ── Database ─────────────────────────────────────────────────────────────────
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=change-me-user

# ── WordPress (must match the DB credentials above) ───────────────────────────
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change-me-user

# Public URL of your site (used to set WP_HOME and WP_SITEURL)
SITE_URL=https://your-domain.example

# ── Cloudflare Tunnel ─────────────────────────────────────────────────────────
# Zero Trust dashboard → Networks → Tunnels → <your tunnel> → Overview → Token
TUNNEL_TOKEN=paste-your-cloudflared-tunnel-token-here

# ── Mail relay (msmtp → Mailpit for dev; real SMTP for prod) ──────────────────
# These defaults point at the Mailpit container — no changes needed for dev.
SMTP_FROM=wordpress@your-domain.example
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_AUTH=off
SMTP_TLS=off
SMTP_USER=
SMTP_PASS=
#
# Production example (uncomment and fill in to switch to a real provider):
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_AUTH=on
# SMTP_TLS=on
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# SMTP_FROM=noreply@your-domain.example
